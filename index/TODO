todo:

- allow user to select with keyboard down from the title

- minimize fouc

- minify js in build.sh

- audit + fix with a11y tooling (`a11y`)

- set up simple behavior tracking (views, actions) for all pages on jczimm.com as well as all pages on *.jczimm.com